# Python setup

- Make sure the root is at `./ml`

- `pip install pipenv`
- `pipenv shell`
- `pipenv install --dev`

# Compile protobuf file

- `pipenv run compile`

# Start server (dev)

- `pipenv run start`
